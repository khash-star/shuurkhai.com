# 🚀 GoDaddy дээр GitHub-ийг Холбох Заавар

## 📋 Энэ заавар юу хийх вэ?

GitHub дээрх кодоо GoDaddy домэйн дээр автоматаар ажиллуулах. GitHub дээр өөрчлөлт хийхэд GoDaddy дээр автоматаар шинэчлэгдэнэ.

---

## 🎯 Арга 1: cPanel Git Version Control (Хамгийн хялбар) ✅

### Алхам 1: GoDaddy cPanel нээх

1. GoDaddy.com дээр нэвтрэх
2. **"My Products"** → **"Web Hosting"** → **"Manage"** дарах
3. **"cPanel Admin"** эсвэл **"cPanel"** дээр дарах
4. cPanel username/password-оор нэвтрэх

### Алхам 2: Git Version Control олох

1. cPanel дээр **"Git Version Control"** эсвэл **"Git"** хэсэг хайх
2. Хэрэв олдохгүй бол:
   - cPanel → **"Software"** → **"Git Version Control"**
   - Эсвэл cPanel дээрх хайлтын талбарт "Git" гэж бичих

### Алхам 3: Repository Clone хийх

1. **"Create"** эсвэл **"Clone"** товч дарах
2. Дараах мэдээллийг оруулах:

   - **Repository URL**: 
     ```
     https://github.com/khash-star/shuurkhai.com.git
     ```
   
   - **Repository Branch**: 
     ```
     main
     ```
   
   - **Clone Directory**: 
     ```
     public_html/shuurkhai_git
     ```
     (Эсвэл хүссэн хавтасны нэр)
   
   - **Repository Name**: 
     ```
     shuurkhai_git
     ```

3. **"Create"** дарах

### Алхам 4: Шалгах

1. Clone хийсний дараа repository-ийн жагсаалтад харагдана
2. Repository-ийн хажууд **"Pull or Deploy"** эсвэл **"Update"** товч байна
3. Дарах нь GitHub-аас хамгийн сүүлийн код татаж авна

---

## 🔄 Код Шинэчлэх (GitHub дээр өөрчлөлт хийсний дараа)

### Арга 1: cPanel Git Version Control ашиглах

1. cPanel → **"Git Version Control"**
2. `shuurkhai_git` repository олох
3. **"Pull or Deploy"** эсвэл **"Update"** дарах
4. **"Update from Remote"** сонгох
5. Branch: `main` сонгох
6. **"Update"** эсвэл **"Pull"** дарах

### Арга 2: cPanel Terminal ашиглах (хэрэв боломжтой бол)

1. cPanel → **"Terminal"** эсвэл **"Web Terminal"**
2. Дараах командуудыг ажиллуулах:

```bash
cd ~/public_html/shuurkhai_git
git pull origin main
```

---

## ⚙️ Database Configuration Тохируулах

GitHub-аас код татаж авсны дараа database нууц үг тохируулах хэрэгтэй:

1. cPanel → **"File Manager"**
2. `public_html/shuurkhai_git/config.php` файлыг олох
3. Edit дээр дарах
4. 19-р мөрийг олох:
   ```php
   $dbpass = getenv('DB_PASS') ?: '';
   ```
5. Дараах байдлаар өөрчлөх:
   ```php
   $dbpass = getenv('DB_PASS') ?: 'ТАНЫ_DATABASE_НУУЦ_ҮГ';
   ```
6. Save хийх

---

## ✅ Шалгах

1. Хөтөч дээр: `https://shuurkhai.com/shuurkhai_git/`
2. Website зөв ажиллаж байгаа эсэхийг шалгах

---

## 🔧 Хэрэв Асуудал Гарвал

### Git Version Control олдохгүй байна

- GoDaddy support-д хандах
- Эсвэл SSH access идэвхжүүлэх хүсэх

### Clone хийхэд алдаа гарч байна

1. Repository URL зөв эсэхийг шалгах
2. GitHub дээр repository public эсэхийг шалгах
3. HTTPS URL ашиглах (SSH биш)

### Pull хийхэд алдаа гарч байна

```bash
# Terminal ашиглах (хэрэв боломжтой бол)
cd ~/public_html/shuurkhai_git
git fetch origin
git reset --hard origin/main
```

**⚠️ Анхаар:** `git reset --hard` нь бүх local өөрчлөлтийг устгана!

---

## 📝 Дүгнэлт

**Хамгийн хялбар арга:**

1. ✅ cPanel → Git Version Control
2. ✅ Repository Clone хийх
3. ✅ Database нууц үг тохируулах
4. ✅ Pull хийж код шинэчлэх

**Дараа нь:**
- GitHub дээр код өөрчлөх
- cPanel дээр Pull хийх
- Website автоматаар шинэчлэгдэнэ!

---

## 🎯 Автомат Deployment (Сонголт)

Хэрэв GitHub дээр push хийхэд автоматаар шинэчлэгдэхийг хүсвэл:

1. GitHub Webhook тохируулах
2. Server дээр deploy script үүсгэх

(Энэ нь илүү төвөгтэй, одоогоор дээрх арга хангалттай)
